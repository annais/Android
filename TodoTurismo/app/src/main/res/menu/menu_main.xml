<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_lista" android:icon="@drawable/ic_action_view_as_list"
            android:title="@string/Lista" />
        <item android:id="@+id/nav_gallery" android:icon="@android:drawable/ic_menu_gallery"
            android:title="@string/galeria" />
        <item android:id="@+id/nav_map" android:icon="@drawable/ic_action_map"
            android:title="@string/mapa" />
    </group>
</menu>
